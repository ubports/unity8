# This is just a small fake QtMir.Application for non-Mir environments
set(PLUG_DIR ${PLUGIN_PRIVATE_MODULE_DIR}/Lomiri/SystemSettings/Wizard/NonMir/QtMir/Application)
install(FILES qmldir OSKController.qml DESTINATION ${PLUG_DIR})
