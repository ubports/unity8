# This is just a small fake Unity.Application for non-Mir environments
set(PLUG_DIR ${PLUGIN_PRIVATE_MODULE_DIR}/Ubuntu/SystemSettings/Wizard/NonMir/Unity/Application)
install(FILES qmldir OSKController.qml DESTINATION ${PLUG_DIR})
