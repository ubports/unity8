#!/bin/sh
set -e

case "$1" in
remove|purge)
    if [ -x /usr/lib/lightdm/lightdm-set-defaults ] ; then
        /usr/lib/lightdm/lightdm-set-defaults --remove --greeter=unity8-greeter --session=ubuntu-touch || true
    fi
    ;;
esac

#DEBHELPER#
